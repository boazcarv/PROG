const b1 = 45
const b2 = 78

let media = (b1 * 2 + b2 * 3) / 5

//console.log(media) = ' + media)
 console.log('A média parcial =' + media)

 if (media >= 60) {
    console.log('Vocé está aprovado(a) com a média =' + media.toFixed(2))
 } else {
    console.log('Avaliação final necessária, sua média =' + media.toFixed(2))
 }